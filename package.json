{
  "name": "comercio-webhook",
  "version": "1.0.0",
  "description": "Webhook para recibir notificaciones de pago",
  "main": "server.js",
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "concurrently \"node server.js\" \"npm run client\"",
    "dev:tunnel": "concurrently \"node server.js\" \"npm run client\" \"npm run ngrok\"",
    "ngrok": "ngrok http 3001",
    "client": "cd client && npm start",
    "install-all": "npm install && cd client && npm install",
    "build": "cd client && npm install && npm run build",
    "render-build": "npm install && cd client && npm install && npm run build"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "uuid": "^9.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
